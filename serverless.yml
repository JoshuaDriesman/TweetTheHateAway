service: tweet-the-hate-away
# app and org for use with dashboard.serverless.com
#app: your-app-name
#org: your-org-name

# You can pin your service to only deploy with a specific Serverless version
# Check out our docs for more details
# frameworkVersion: "=X.X.X"

provider:
  name: aws
  runtime: nodejs12.x
  stage: dev
  region: us-east-1
  profile: serverless-framework
  memorySize: 128
  iamRoleStatements:
    - Effect: "Allow"
      Action:
        - "secretsmanager:GetSecretValue"
      Resource: ${env:TWITTER_SECRET_ARN}
  environment:
    TWITTER_SECRET_ARN: ${env:TWITTER_SECRET_ARN}

plugins:
  - serverless-webpack
  - serverless-offline

custom:
  webpack:
    webpack: webpack.config.js

# you can add packaging information here
#package:
#  include:
#    - include-me.js
#    - include-me-dir/**
#  exclude:
#    - exclude-me.js
#    - exclude-me-dir/**

layers:
  common:
    path: app/common
    description: Common utility functions for the application.

functions:
  twitter-challenge-response:
    handler: app/twitter-web-hook.twitterCrcResponder
    layers:
      - { Ref: CommonLambdaLayer }
    events:
      - httpApi:
          path: /twitter-web-hook
          method: GET
# you can add CloudFormation resource templates here
#resources:
#  Resources:
#    NewResource:
#      Type: AWS::S3::Bucket
#      Properties:
#        BucketName: my-new-bucket
#  Outputs:
#     NewOutput:
#       Description: "Description for the output"
#       Value: "Some output value"
